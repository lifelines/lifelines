# make sure LANG is UTF-8
env LANG=UTF-8
env TERM=xterm
# try dbverify and lldump
# lltest not checked as it requires empty database, see math/check_lltest
post dbverify -a testdb
post lldump -a testdb 
